# Empty uses the current browser origin and the /api/v1 path.
# For a separate API domain, set only the origin (no trailing /api/v1).
VITE_API_ORIGIN=

# Used only by the local Vite development proxy.
DEV_API_PROXY_TARGET=http://localhost:8000
